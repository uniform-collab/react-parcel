const FeaturedProduct = ({ eyebrowText, title }) => {
  return (
    <div style={{ paddingTop: "200px", paddingBottom: "200px" }}>
      FeaturedProduct: {eyebrowText} {title}
    </div>
  );
};

export default FeaturedProduct;
